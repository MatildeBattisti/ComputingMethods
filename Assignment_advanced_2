
file_path = 'voltage_data.txt'

def adc_to_voltage(adc):
    """
    Convert ADC counts to a physical voltage 
    """
    return 1.653 * adc * 0.456

input_file = open(file_path)
for line in input_file.readlines():
    timestamp, adc = line.split()
    timestamp = float(tymestamp)
    adc = int(adc)
    voltage = adc_to_voltage
    print(timestamp, ad, type(timestamp), type(adc))

"""
Non permette l'interpolazione. Come si corregge?
"""